const express = require('express');
const app = express();
app.listen(3000);
const say = require('say');
var Twit = require('twit');
var tweetText;
var T = new Twit({
    consumer_key: 'Cpz4uRvFIWfWnzViRpENH7yxs',
    consumer_secret: 'talqkfx7CIB85G3GOa4eOT9YAqnmLwmobdxhxCJjKjDVxKNJxz',
    app_only_auth: true
})
T.get('search/tweets', {
    q: 'realDonaldTrump', count: 100 
}, function (err, data, response) {
    tweetText = data.statuses[Math.floor((Math.random() * 100) + 1)].text;
    say.speak(tweetText, 'Alex', 1);
    console.log(tweetText);
})
app.get('/', (req, res) => res.send(tweetText));
